apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-devops-ingress
  namespace: demo-devops
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: "154.38.187.15"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-devops-nodejs-svc
                port:
                  number: 80